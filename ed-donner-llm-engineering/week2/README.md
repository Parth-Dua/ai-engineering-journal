# Week 2 — Multi-Modal AI, Function Calling & Gradio

**Project:** Multi-Modal Customer Support Assistant — handles text, image uploads, and audio with a Gradio UI and function calling.

**What I learned:**
- Multi-modal API calls: sending images and audio alongside text
- Function calling / tool use — how LLMs invoke external functions
- Building interactive UIs with Gradio
- HuggingFace model hub — finding, loading, and running open-source models
- The basic agent loop: decide → call tool → observe → respond

**Notebooks:** `day1` → multi-modal inputs · `day2` → audio + transcription · `day3` → function calling · `day4` → Gradio UI · `day5` → full assistant
