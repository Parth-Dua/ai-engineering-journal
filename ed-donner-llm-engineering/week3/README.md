# Week 3 — Open-Source Models, Embeddings & HuggingFace

**Project:** AI Meeting Minutes Tool — transcribes audio recordings and extracts structured action items using open-source + Frontier models.

**What I learned:**
- What embeddings actually are and why they enable semantic search
- Cosine similarity and vector search built from scratch
- HuggingFace Transformers and Pipelines in depth
- Running models on Google Colab with GPU
- Reading model leaderboards and benchmarks to pick the right model
- Audio transcription with Whisper-family models

**Notebooks:** `day1` → embeddings + cosine similarity · `day2` → HuggingFace pipelines · `day3` → model selection and benchmarks · `day4` → transcription pipeline · `day5` → full minutes tool
